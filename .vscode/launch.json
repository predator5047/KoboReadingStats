{
    
    
    "configurations": [
      {
        "type": "cppdbg",
        "request": "launch",
        "name": "Launch Program",
        "program": "${workspaceFolder}/builddir/src/koboreadingstats",
        "preLaunchTask": "build",
        "cwd": "${workspaceFolder}/builddir/src/",
      }
    ]
    }